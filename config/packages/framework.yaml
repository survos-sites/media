# see https://symfony.com/doc/current/reference/configuration/framework.html
framework:
    secret: '%env(APP_SECRET)%'

    # Trust the reverse proxy (nginx/caddy) that terminates TLS.
    # REMOTE_ADDR trusts whatever IP directly connects — safe when the proxy
    # is on the same host or a known internal network.
    trusted_proxies: '127.0.0.1,REMOTE_ADDR'
    trusted_headers: ['x-forwarded-for', 'x-forwarded-host', 'x-forwarded-proto', 'x-forwarded-port', 'x-forwarded-prefix']

    # Note that the session will be started ONLY if you read or write from it.
    session: true
    router:
        default_uri: 'https://mediary.survos.com'

    #esi: true
    #fragments: true
    http_client:
        default_options:
            max_redirects: 8
            timeout: 30
            proxy: '%env(HTTP_PROXY)%'

#    error_controller: App\Controller\ErrorController::show

when@test:
    framework:
        test: true
        session:
            storage_factory_id: session.storage.factory.mock_file

when@dev:
    framework:
        router:
            default_uri: 'https://media.wip'

